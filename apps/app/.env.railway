# Variáveis de ambiente para Railway

# Configuração do Next.js
NEXT_PUBLIC_APP_URL=https://cureclinica-tricologia.up.railway.app
NEXT_PUBLIC_APP_VERSION=1.0.0

# Configuração do Supabase (precisará ser preenchido com os valores reais)
NEXT_PUBLIC_SUPABASE_URL=${SUPABASE_URL}
NEXT_PUBLIC_SUPABASE_ANON_KEY=${SUPABASE_ANON_KEY}
SUPABASE_SERVICE_ROLE_KEY=${SUPABASE_SERVICE_ROLE_KEY}

# Configuração de autenticação
NEXT_PUBLIC_AUTH_ENABLED=true
NEXTAUTH_URL=https://cureclinica-tricologia.up.railway.app
NEXTAUTH_SECRET=${NEXTAUTH_SECRET}

# Upload de imagens
UPLOADTHING_SECRET=${UPLOADTHING_SECRET}
UPLOADTHING_APP_ID=${UPLOADTHING_APP_ID}

# Email
EMAIL_SERVER_HOST=${EMAIL_SERVER_HOST}
EMAIL_SERVER_PORT=${EMAIL_SERVER_PORT}
EMAIL_SERVER_USER=${EMAIL_SERVER_USER}
EMAIL_SERVER_PASSWORD=${EMAIL_SERVER_PASSWORD}
EMAIL_FROM=${EMAIL_FROM}

# Analytics
NEXT_PUBLIC_ANALYTICS_URL=${ANALYTICS_URL}
NEXT_PUBLIC_ANALYTICS_SITE_ID=${ANALYTICS_SITE_ID}

# Monitoring
NEXT_PUBLIC_SENTRY_DSN=${SENTRY_DSN}
